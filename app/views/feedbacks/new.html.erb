<%= page_title(t('.title')) %>
<%= render 'shared/errors_messages', model: @feedback %>

<%= render 'shared/feedback_form', model: @feedback, url: my_event_feedbacks_path(my_event_id: @my_event_id) %>