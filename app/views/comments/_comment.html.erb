<div class="bg-white p-6 rounded-lg shadow-md mb-6">
    <div class="bg-gray-50 p-5 rounded-lg shadow-sm mb-5">
      <p class="text-lg text-gray-800 mb-2"><%= comment.content %></p>
      <p class="text-gray-600 text-sm font-medium"><%= comment.user.name %></p>
      <p class="text-gray-500 text-xs"><%= l(comment.created_at, format: :short) %></p>
    </div>
</div>

